#!/bin/bash

# The purpose of this script is to copy the generated kubernetes manifest files from the primary build process
# an place them in the opinionated namespace-per-service format for the ACM Namespace repositories

