apiVersion: skaffold/v2beta26
kind: Config
metadata: 
  name: ledger-db
requires:
  - configs: [config]
    path: ../../k8s/config/skaffold.yaml
build:
  artifacts:
    - image: ledger-db
      context: .
deploy:
  kustomize:
    paths: ["k8s/base"]
profiles:
  - name: staging
    deploy:
      kustomize:
        paths: ["k8s/staging"]
  - name: production
    deploy:
      kustomize:
        paths: ["k8s/production"]
